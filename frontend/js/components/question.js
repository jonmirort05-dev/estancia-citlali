class Question {

    constructor(title, type, options = []) {

        this.title = title;

        this.type = type;

        this.options = options;

        this.placeholder = "";

        this.answer = null;

    }

}